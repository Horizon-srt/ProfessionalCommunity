'use client';
import ServiceEdit from '@/components/ServiceEdit';
import React from 'react';

const Family: React.FC = () => {
  return <ServiceEdit title='Create new Service' sid='-1' />;
};

export default Family;
